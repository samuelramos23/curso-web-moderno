const raio = 5.6
const area = Math.PI * Math.pow(raio, 2) // Math.PI é uma constante que representa o valor de pi
// Math.pow() eleva o número ao quadrado, ou seja, multiplica o número por ele mesmo

console.log(area)
console.log(typeof Math)